define([
    'connecta.graph',
    'graph/configuration/service/configuration-service',
    'graph/configuration/controller/modal-rest-response',
], function (graph) {
    return graph.lazy.controller('ConfigurationFormController', function ($scope, $window, ConfigurationService, $location, $routeParams, util, notify, $uibModal) {
    });
});